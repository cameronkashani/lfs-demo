# Last Fan Standing — demo

A single self-contained page. Fonts, sprites and card art are inlined, so it
runs with no external requests.

Built from the working copy at `~/Desktop/lfs-drop-ours` with
`python3 tools/build-artifact.py`. Only the built page is published here, not
the source tree.
